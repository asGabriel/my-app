list:
  post:
    operationId: listDebts
    summary: Lista os débitos do período
    tags:
      - Debt
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: "../schemas/debt.yaml#/DebtFilters"
    responses:
      "200":
        description: Lista de débitos
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: "../schemas/debt.yaml#/Debt"

create:
  post:
    operationId: createDebt
    summary: Cria um novo débito
    tags:
      - Debt
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: "../schemas/debt.yaml#/CreateDebtRequest"
    responses:
      "201":
        description: Débito criado com sucesso
        content:
          application/json:
            schema:
              $ref: "../schemas/debt.yaml#/Debt"

listInstallments:
  post:
    operationId: listInstallments
    summary: Lista as parcelas dos débitos
    tags:
      - Debt
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: "../schemas/debt.yaml#/InstallmentFilters"
    responses:
      "200":
        description: Lista de parcelas
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: "../schemas/debt.yaml#/Installment"

update:
  patch:
    operationId: updateDebt
    summary: Atualiza um débito existente
    tags:
      - Debt
    parameters:
      - name: debtId
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: ID do débito a ser atualizado
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: "../schemas/debt.yaml#/UpdateDebtRequest"
    responses:
      "200":
        description: Débito atualizado com sucesso
        content:
          application/json:
            schema:
              $ref: "../schemas/debt.yaml#/Debt"
